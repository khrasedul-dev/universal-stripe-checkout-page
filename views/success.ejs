<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css?family=Inter:400,600&display=swap" rel="stylesheet">
  <style>
    body { margin: 0; font-family: 'Inter', Arial, sans-serif; background: linear-gradient(120deg, #e0ffe7 0%, #f8fafc 100%); color: #222; min-height: 100vh; display: flex; align-items: center; justify-content: center; }
    .container { background: #fff; box-shadow: 0 8px 32px rgba(0,0,0,0.10); border-radius: 24px; padding: 56px 40px; max-width: 420px; width: 100%; text-align: center; position: relative; }
    .icon { font-size: 3.5rem; color: #22c55e; margin-bottom: 18px; }
    h1 { font-size: 2rem; font-weight: 700; color: #22c55e; margin-bottom: 12px; }
    p { font-size: 1.1rem; color: #444; margin-bottom: 18px; }
    .details { font-size: 1rem; color: #555; margin-bottom: 28px; }
    .info-list { list-style: none; padding: 0; margin: 0 0 24px 0; }
    .info-list li { background: #e0ffe7; color: #222; border-radius: 8px; margin-bottom: 10px; padding: 10px 16px; font-size: 0.98rem; display: flex; align-items: center; gap: 8px; }
    .info-list .emoji { font-size: 1.2rem; }
    .cta-btn { display: inline-block; background: linear-gradient(90deg, #22c55e 0%, #16a34a 100%); color: #fff; font-weight: 600; padding: 12px 32px; border-radius: 8px; text-decoration: none; font-size: 1rem; box-shadow: 0 2px 8px rgba(34,197,94,0.10); transition: background 0.2s, box-shadow 0.2s; border: none; cursor: pointer; }
    .cta-btn:hover { background: linear-gradient(90deg, #16a34a 0%, #22c55e 100%); box-shadow: 0 6px 24px rgba(34,197,94,0.18); }
    @media (max-width: 600px) { .container { padding: 18px 4px; max-width: 98vw; } h1 { font-size: 1.3rem; } .icon { font-size: 2.2rem; } .cta-btn { padding: 10px 18px; font-size: 0.95rem; } }
  </style>
</head>
<body>
  <div class="container">
    <div class="icon">✅</div>
    <h1><%= title %></h1>
    <p><%= message %></p>
    <div class="details"><%= details %></div>
    <ul class="info-list">
      <li><span class="emoji">🕒</span> Payment will processed instantly</li>
      <li><span class="emoji">🔒</span> Your transaction is secure</li>
      <li><span class="emoji">💬</span> For support, contact us anytime</li>
    </ul>
    <a href="https://t.me/<%= username %>" class="cta-btn"> <span class="cta-icon">🤖</span> Back to Bot</a>
  </div>
</body>
</html>
